<h1>Jobs</h1>
{% for result in searchResults %}
  <div>
    <h2>{{ loop.index }}. <a href="node/{{ result.nid }}">{{ result.title|raw }}</a></h2>
    <p>{{ result.employer }}</p>
    <p>{{ result.description|raw }}</p>
  </div>
{% endfor %}
